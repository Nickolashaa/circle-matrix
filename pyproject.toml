[project]
name = "circle-matrix"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.8",
    "mypy>=1.19.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.1",
]
